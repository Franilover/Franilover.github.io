Este tar es INCREMENTAL — se aplica encima de los anteriores
(garlia_rediseno_completo.tar.gz y garlia_click_nombre_abre_panel.tar.gz).
Aplicalos en orden si todavía no lo hiciste.

Antes de descomprimir, borrá estos 3 archivos (huérfanos, ya reemplazados):

  rm src/domains/garlia/_shared/TarjetaGrupoVinculado.tsx
  rm src/domains/garlia/flora/SelectorOrganoPlanta.tsx
  rm src/domains/garlia/minerales/SelectorFormacionMineral.tsx

Después descomprimir parado en la raíz de domains/garlia/:

  tar -xzf garlia_unificacion_tarjetas.tar.gz -C src/domains/garlia/

Archivos que trae (5, todos pisan o crean):
  _shared/TarjetaFormacionOrgano.tsx   (NUEVO — reemplaza TarjetaGrupoVinculado,
                                         FormacionCard de Minerales, y OrganoCard
                                         de Flora, los 3 en uno solo)
  _shared/SeccionGruposVinculados.tsx  (reemplaza — nuevo flujo Crear/Usar)
  flora/FloraEditor.tsx                (reemplaza — Órganos usa el componente
                                         compartido)
  minerales/MineralEditor.tsx          (reemplaza — Formaciones usa el
                                         componente compartido)
  items/EditorItem.tsx                 (reemplaza — Crear abre el panel
                                         automáticamente)

Qué cambió (3 pedidos en un tar):

1. UN SOLO COMPONENTE para Órganos y Formaciones (antes: 3 implementaciones
   casi idénticas — TarjetaGrupoVinculado en Items, FormacionCard en
   MineralEditor, OrganoCard en FloraEditor). Ahora los 3 usan
   TarjetaFormacionOrgano vía SeccionGruposVinculados. Mismo diseño exacto
   en Biología (via GridCatalogoGrupo, sin cambios ahí), Física, Flora,
   Minerales, e Items.

2. Botón "+" rediseñado — ahora es un menú con 2 opciones claras:
   - "Crear nuevo": crea un registro vacío en el catálogo, lo vincula, y
     ABRE DIRECTO el panel flotante (GrupoCompuestoPanelFlotante) para
     cargar los compuestos ahí. Ya no queda una tarjeta vacía editable
     inline esperando que el usuario la complete a mano.
   - "Usar existente": abre un picker en overlay que muestra cada opción
     disponible (del catálogo, no vinculada todavía a esta entidad) como
     TARJETA REAL con su fórmula/notas visibles — no como lista de texto.
     Clickear una tarjeta la vincula. Estas tarjetas del picker son de
     SOLO SELECCIÓN: no se puede editar ni agregar compuestos desde ahí
     (los botones de edición están deshabilitados) — para editar el
     contenido hay que abrir el panel flotante de la Formación/Órgano ya
     vinculado, fuera del picker.

3. Órganos de Flora y Formaciones de Minerales ya no tienen su propio
   picker de header con botón "+" separado — el "+" de SeccionGruposVinculados
   cubre ambos casos (Crear/Usar), así que se sacó el botón y picker viejo
   del header de esas tabs (Procesos sigue con su botón propio, sin cambios,
   porque Procesos no fue parte de este pedido).

Después de aplicar, correr build/tsc para confirmar que no quedó ninguna
referencia rota.
